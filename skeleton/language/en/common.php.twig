<?php
/**
 *
 * {{ EXTENSION.extension_display_name }}. An extension for the phpBB Forum Software package.
 *
 * @copyright (c) {{ "now"|date("Y") ~ (AUTHORS.0.author_name ? ', ' ~ AUTHORS.0.author_name) ~ (AUTHORS.0.author_homepage ? ', ' ~ AUTHORS.0.author_homepage) }}
 * @license GNU General Public License, version 2 (GPL-2.0)
 *
 */

if (!defined('IN_PHPBB'))
{
	exit;
}

if (empty($lang) || !is_array($lang))
{
	$lang = [];
}

// DEVELOPERS PLEASE NOTE
//
// All language files should use UTF-8 as their encoding and the files must not contain a BOM.
//
// Placeholders can now contain order information, e.g. instead of
// 'Page %s of %s' you can (and should) write 'Page %1$s of %2$s', this allows
// translators to re-order the output of data while ensuring it remains correct
//
// You do not need this where single placeholders are used, e.g. 'Message %d' is fine
// equally where a string contains only two placeholders which are used to wrap text
// in a url you again do not need to specify an order e.g., 'Click %sHERE%s' is fine
//
// Some characters you may want to copy&paste:
// ’ » “ ” …
//

$lang = array_merge($lang, [

{% if COMPONENT.controller or COMPONENT.acp %}
	'{{ EXTENSION.extension_name|upper }}_HELLO'		=> 'Hello %s!',
	'{{ EXTENSION.extension_name|upper }}_GOODBYE'		=> 'Goodbye %s!',

{% endif %}
{% if COMPONENT.phplistener %}
	'{{ EXTENSION.extension_name|upper }}_EVENT'		=> ' :: {{ EXTENSION.extension_name|capitalize }} Event :: ',

{% endif %}
{% if COMPONENT.acp %}
	'ACP_{{ EXTENSION.extension_name|upper }}_GOODBYE'			=> 'Should say goodbye?',
	'ACP_{{ EXTENSION.extension_name|upper }}_SETTING_SAVED'	=> 'Settings have been saved successfully!',

{% endif %}
{% if COMPONENT.notification %}
	'{{ EXTENSION.vendor_name|upper }}_{{ EXTENSION.extension_name|upper }}_NOTIFICATION'	=> '{{ EXTENSION.extension_display_name|e }} notification',

{% endif %}
{% if COMPONENT.controller %}
	'{{ EXTENSION.extension_name|upper }}_PAGE'			=> '{{ EXTENSION.extension_name|capitalize }} Page',
	'VIEWING_{{ EXTENSION.vendor_name|upper }}_{{ EXTENSION.extension_name|upper }}'			=> 'Viewing {{ EXTENSION.extension_display_name|e }} page',

{% endif %}
]);
